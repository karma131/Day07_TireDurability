# Báo Cáo Cá Nhân — Lab 7: Embedding & Vector Store

**Họ tên:** Đào Văn Đa

**Mã sinh viên:** 2A202601089

**Nhóm/Lớp:** 2A

**Ngày cập nhật báo cáo:** 03/08/2026

---

## 1. Khởi động (Warm-up) — Cá nhân (5 điểm)

### 1.1. Độ tương tự cosine

**Độ tương tự cosine cao nghĩa là gì?**

Hai vector embedding có cosine similarity cao khi chúng hướng gần giống nhau
trong không gian vector. Trong thực tế, điều này thường cho biết hai văn bản có
chủ đề hoặc ý nghĩa gần nhau, dù cách dùng từ có thể không hoàn toàn giống nhau.

**Ví dụ có độ tương tự cao**

- Câu A: “Sinh viên có thể gia hạn sách thư viện trực tuyến.”
- Câu B: “Người học được phép kéo dài thời hạn mượn sách qua hệ thống online.”
- Lý do: cả hai đều diễn đạt việc sinh viên gia hạn thời gian mượn sách bằng
  dịch vụ trực tuyến.

**Ví dụ có độ tương tự thấp**

- Câu A: “Sinh viên có thể gia hạn sách thư viện trực tuyến.”
- Câu B: “Sáng nay trời có mưa lớn ở khu vực miền Trung.”
- Lý do: hai câu nói về hai chủ đề và hai nhóm khái niệm không liên quan.

**Tại sao ưu tiên cosine similarity hơn khoảng cách Euclid cho text embedding?**

Cosine similarity tập trung vào góc giữa hai vector, tức hướng biểu diễn ngữ
nghĩa, và ít bị ảnh hưởng bởi độ lớn của vector. Khoảng cách Euclid phụ thuộc
nhiều vào độ lớn nên hai vector cùng hướng nhưng khác chuẩn có thể bị xem là xa
nhau; điều này thường không phù hợp bằng cosine khi so sánh văn bản.

### 1.2. Bài toán tính số lượng chunk

Với `document_length = 10.000`, `chunk_size = 500`, `overlap = 50`:

```text
step = chunk_size - overlap = 500 - 50 = 450
chunk_count = ceil((document_length - overlap) / step)
            = ceil((10.000 - 50) / 450)
            = ceil(9.950 / 450)
            = ceil(22,111...)
            = 23 chunks
```

Khi tăng `overlap` lên 100:

```text
step = 500 - 100 = 400
chunk_count = ceil((10.000 - 100) / 400)
            = ceil(9.900 / 400)
            = ceil(24,75)
            = 25 chunks
```

Số chunk tăng từ **23 lên 25** vì bước dịch cửa sổ giảm từ 450 còn 400 ký tự.
Overlap lớn hơn giúp giữ lại ngữ cảnh nằm ở ranh giới giữa hai chunk và giảm
nguy cơ cắt mất một ý quan trọng. Đổi lại, hệ thống phải lưu nhiều dữ liệu hơn,
tốn thêm chi phí embedding và có thể trả về các kết quả trùng lặp.

---

## 2. Hướng tiếp cận của tôi (My Approach) — Cá nhân (10 điểm)

### 2.1. Các hàm chunking

**`SentenceChunker.chunk`**

Tôi dùng biểu thức chính quy `(?<=[.!?])\s+` để nhận diện khoảng trắng ngay sau
dấu kết câu `.`, `!` hoặc `?`. Cách này giữ dấu câu trong câu gốc, hỗ trợ cả dấu
cách và xuống dòng, sau đó gom tối đa `max_sentences_per_chunk` câu vào mỗi
chunk. Văn bản rỗng hoặc chỉ chứa khoảng trắng trả về danh sách rỗng; giá trị số
câu tối đa nhỏ hơn 1 được chuẩn hóa thành 1.

**`RecursiveChunker.chunk` và `_split`**

Thuật toán thử lần lượt các separator theo độ ưu tiên:
`"\n\n"`, `"\n"`, `". "`, `" "` và cuối cùng là `""`. Các mảnh nhỏ được ghép
vào bộ đệm đến gần `chunk_size`; mảnh vượt kích thước được xử lý đệ quy bằng
separator tiếp theo. Base case là đoạn đã không vượt `chunk_size`; nếu hết
separator hoặc gặp separator rỗng thì đoạn được cắt cứng theo số ký tự, bảo đảm
thuật toán luôn kết thúc.

**`compute_similarity`**

Hàm tính tích vô hướng rồi chia cho tích độ lớn Euclid của hai vector. Nếu một
vector có độ lớn bằng 0, hàm trả về `0.0` để tránh chia cho 0. Hai vector khác số
chiều bị từ chối bằng `ValueError`, tránh việc `zip` âm thầm bỏ mất phần tử.

**`ChunkingStrategyComparator.compare`**

Comparator chạy cùng một văn bản qua `FixedSizeChunker`, `SentenceChunker` và
`RecursiveChunker`. Với mỗi chiến lược, kết quả gồm danh sách chunk, tổng số
chunk và độ dài trung bình; trường hợp văn bản rỗng có `avg_length = 0.0`.

### 2.2. Lớp `EmbeddingStore`

**`add_documents` và `search`**

Mỗi `Document` được chuyển thành record gồm ID nội bộ duy nhất, nội dung,
metadata và embedding. Nếu ChromaDB có sẵn, record được đưa vào collection dùng
inner product; nếu không, record được lưu trong danh sách trong bộ nhớ. Khi tìm
kiếm, truy vấn được embedding một lần, tính dot product với các record, sắp xếp
giảm dần theo `score` và chỉ trả về tối đa `top_k`.

**`search_with_filter` và `delete_document`**

Metadata được lọc **trước** khi tính độ tương tự để chỉ xếp hạng các ứng viên
đúng toàn bộ cặp khóa–giá trị yêu cầu. `delete_document` xóa tất cả record có
`metadata["doc_id"]` trùng ID tài liệu và trả về `True` khi thực sự có phần tử bị
xóa, ngược lại trả về `False`. Khi metadata đầu vào chưa có `doc_id`, store tự
gắn ID của `Document`, vì vậy chức năng xóa vẫn nhất quán.

### 2.3. Tác tử `KnowledgeBaseAgent`

`answer` truy xuất top-k chunk, đánh số từng nguồn rồi đặt chúng trong thẻ
`<context>`; câu hỏi được đặt riêng trong thẻ `<question>`. Prompt yêu cầu mô
hình kiểm kê đầy đủ số lượng, thời hạn, điều kiện, ngoại lệ và hậu quả trước khi
trả lời, đồng thời chỉ sử dụng thông tin đã truy xuất. Sau khi gọi `llm_fn`, agent
chọn đoạn liên quan nhất trong chunk top-1 và đính kèm làm thông tin đối chiếu
trực tiếp, giúp hạn chế bỏ sót chi tiết và tăng khả năng kiểm chứng. Nếu store
rỗng, prompt vẫn được tạo với thông báo không tìm thấy ngữ cảnh thay vì gây lỗi.

### 2.4. Các quyết định xử lý trường hợp biên

- Từ chối `chunk_size <= 0` và `overlap` âm hoặc không nhỏ hơn `chunk_size`.
- Không tạo chunk rỗng từ văn bản rỗng hoặc separator liên tiếp.
- `top_k <= 0` trả về danh sách rỗng.
- Thêm nhiều document cùng ID vẫn có ID record nội bộ riêng.
- Sao chép metadata khi lưu và khi trả kết quả để hạn chế thay đổi ngoài ý muốn.
- Hỗ trợ cả backend trong bộ nhớ và ChromaDB tùy chọn.

---

## 3. Hoàn thiện code (Core Implementation) — Cá nhân (30 điểm)

### 3.1. Các thành phần đã hoàn thành

- [x] `SentenceChunker`
- [x] `RecursiveChunker`
- [x] `compute_similarity`
- [x] `ChunkingStrategyComparator`
- [x] `EmbeddingStore.__init__`
- [x] `EmbeddingStore._make_record`
- [x] `EmbeddingStore._search_records`
- [x] `EmbeddingStore.add_documents`
- [x] `EmbeddingStore.search`
- [x] `EmbeddingStore.get_collection_size`
- [x] `EmbeddingStore.search_with_filter`
- [x] `EmbeddingStore.delete_document`
- [x] `KnowledgeBaseAgent.__init__`
- [x] `KnowledgeBaseAgent.answer`

### 3.2. Kết quả kiểm thử

Lệnh đã chạy:

```bash
python -m pytest tests -v
```

Kết quả:

```text
============================= test session starts =============================
platform win32 -- Python 3.12.7, pytest-9.1.1
collected 44 items

tests/test_solution.py ............................................       [100%]

============================= 44 passed in 0.12s ==============================
```

**Số lượng bài test vượt qua:** **44 / 44**

Ghi chú: chuẩn chính thức của lab là Python 3.11; mã đã được viết bằng cú pháp
tương thích Python 3.11. Lần xác minh trong workspace này dùng Python 3.12.7 do
đó là interpreter đang được cài sẵn.

---

## 4. Dự đoán độ tương tự — Giai đoạn 2 (5 điểm)

Tôi ghi dự đoán trước khi chạy `text-embedding-3-small`. Ngưỡng dùng để quy đổi
điểm thực tế thành nhãn là `0,5`: từ `0,5` trở lên là cao, dưới `0,5` là thấp.

<!-- 2A202601089_DAOVANDA_SIMILARITY_RESULTS_START -->
| Cặp | Câu A | Câu B | Dự đoán | Điểm thực tế | Đúng? |
|---:|---|---|---|---:|---|
| 1 | Sinh viên có thể gia hạn sách thư viện trực tuyến. | Người học được phép kéo dài thời hạn mượn sách qua website thư viện. | Cao | 0.725871 | Có |
| 2 | Mỗi nhóm được đặt phòng học tối đa hai giờ. | Thời lượng tối đa cho một phiên đặt phòng nhóm là 120 phút. | Cao | 0.591021 | Có |
| 3 | Thiết bị quá hạn trên năm ngày được xem là thất lạc. | Thời tiết Hà Nội hôm nay có mưa lớn. | Thấp | 0.288613 | Có |
| 4 | Đăng nhập VinUni ID để truy cập tài nguyên từ xa. | Sinh viên sử dụng tài khoản VinUni khi đọc cơ sở dữ liệu ngoài trường. | Cao | 0.634631 | Có |
| 5 | Thư viện có không gian học tập mở 24/7. | Người mượn phải trả chi phí thay thế thiết bị thất lạc. | Thấp | 0.207318 | Có |

**Kết quả:** 5 / 5 dự đoán đúng theo ngưỡng 0,5.

**Kết quả bất ngờ nhất và suy ngẫm:** Cả năm dự đoán đều khớp với ngưỡng đã chọn. Cặp 2 gần ranh giới nhất với score 0.591021; kết quả cho thấy nhãn cao/thấp còn phụ thuộc ngưỡng đánh giá, còn cosine score mới là thông tin chi tiết hơn.
<!-- 2A202601089_DAOVANDA_SIMILARITY_RESULTS_END -->

---

## 5. Kết quả truy xuất cá nhân — Giai đoạn 2 (10 điểm)

### 5.1. Cấu hình của 2A202601089_DAOVANDA

- **Mã định danh:** `2A202601089_DAOVANDA`
- **Vai trò trong nhóm:** Thành viên 4
- **Corpus:** `data/vinuni_library_services`
- **Số tài liệu:** 7
- **Chiến lược:** `RecursiveChunker(chunk_size=500)`
- **Thứ tự separator:** `"\n\n"` → `"\n"` → `". "` → `" "` → `""`
- **Embedding:** `text-embedding-3-small`
- **LLM:** `gpt-4o-mini` (được khóa cố định trong script)
- **Top-k:** 3
- **Metadata filter câu 1:** `{"audience": "student"}`

Tôi chọn RecursiveChunker vì tài liệu chính sách và FAQ có cấu trúc theo tiêu
đề, đoạn và câu. Thuật toán ưu tiên giữ nguyên đơn vị văn bản lớn có ý nghĩa,
chỉ dùng separator nhỏ hơn khi đoạn vượt 500 ký tự; do đó ít cắt giữa câu hơn
fixed-size baseline nhưng vẫn khống chế kích thước đầu vào retrieval.

### 5.2. Thống kê chunk đã xác minh cục bộ

| Tài liệu | Số ký tự | Số chunk | Độ dài trung bình | Dài nhất |
|---|---:|---:|---:|---:|
| `vinuni-graduate-faculty-borrowing` | 1.089 | 3 | 361,67 | 459 |
| `vinuni-library-access-policy` | 2.369 | 7 | 336,71 | 493 |
| `vinuni-library-faq` | 1.306 | 3 | 434,00 | 482 |
| `vinuni-library-learning-support` | 1.012 | 3 | 336,00 | 444 |
| `vinuni-library-room-booking` | 947 | 2 | 472,50 | 482 |
| `vinuni-library-spaces` | 830 | 2 | 414,00 | 470 |
| `vinuni-undergraduate-borrowing` | 1.400 | 3 | 465,33 | 492 |
| **Toàn corpus** | **8.953** | **23** | **387,87** | **493** |

Tất cả 23 chunk đều không vượt `chunk_size=500`. Độ dài trung bình khoảng 388
ký tự cho thấy chiến lược tận dụng khá tốt giới hạn mà vẫn giữ các mục nội dung
liền mạch.

### 5.3. So sánh đường cơ sở trên 3 tài liệu

| Tài liệu | Chiến lược | Số chunk | Độ dài trung bình |
|---|---|---:|---:|
| `vinuni-library-access-policy` | Fixed size | 6 | 436,50 |
|  | Sentence | 9 | 261,44 |
|  | **Recursive** | **7** | **336,71** |
| `vinuni-library-faq` | Fixed size | 3 | 468,67 |
|  | Sentence | 5 | 259,60 |
|  | **Recursive** | **3** | **434,00** |
| `vinuni-undergraduate-borrowing` | Fixed size | 3 | 500,00 |
|  | Sentence | 5 | 278,40 |
|  | **Recursive** | **3** | **465,33** |

SentenceChunker tạo nhiều chunk ngắn nhất và có khả năng tách các điều kiện liên
quan sang các chunk khác nhau. FixedSizeChunker tạo ít chunk nhưng có thể cắt
giữa câu. RecursiveChunker tạo số chunk gần fixed-size trên FAQ và tài liệu mượn
sách, đồng thời tách chính sách dài thành bảy đoạn theo cấu trúc tự nhiên; đây là
sự cân bằng phù hợp giữa độ mạch lạc và kích thước.

### 5.4. Kết quả 5 benchmark

Script thực nghiệm:

```bash
python -X utf8 scripts/evaluate_member4_recursive.py
```

Script dùng đúng 5 câu hỏi trong `tests/BENCHMARK_LIBRARY.md`, lưu nguyên top-3,
score, nguồn, câu trả lời agent và điểm theo rubric vào:

```text
results/member4_recursive/results.json
results/member4_recursive/RESULTS.md
```

<!-- 2A202601089_DAOVANDA_BENCHMARK_RESULTS_START -->
| # | Query rút gọn | Top-1 | Score | Evidence trong top-3? | Điểm |
|---:|---|---|---:|---|---:|
| 1 | Quyền mượn sinh viên đại học | `vinuni-undergraduate-borrowing` | 0.689121 | Có (rank 1) | 2 / 2 |
| 2 | Giới hạn đặt phòng học nhóm | `vinuni-library-room-booking` | 0.560913 | Có (rank 1) | 2 / 2 |
| 3 | Mượn và trả thiết bị | `vinuni-library-access-policy` | 0.674056 | Có (rank 1) | 2 / 2 |
| 4 | Truy cập tài nguyên ngoài trường | `vinuni-undergraduate-borrowing` | 0.717796 | Có (rank 1) | 2 / 2 |
| 5 | Đã trả sách nhưng vẫn báo quá hạn | `vinuni-library-faq` | 0.670180 | Có (rank 1) | 2 / 2 |

**Tổng điểm:** **10 / 10**.

**Phân tích metadata filter câu 1:** Có filter, top-3 lần lượt là `['vinuni-undergraduate-borrowing', 'vinuni-undergraduate-borrowing', 'vinuni-library-faq']`; không filter là `['vinuni-library-access-policy', 'vinuni-undergraduate-borrowing', 'vinuni-graduate-faculty-borrowing']`. So sánh này cho biết bộ lọc có loại được tài liệu sai đối tượng cao học/giảng viên hay không.

**Câu trả lời của agent:**

- **Câu 1:** Sinh viên đại học được mượn tối đa 3 tài liệu, mỗi tài liệu trong 2 tuần. Sách được gia hạn một lần thêm 1 tuần nếu chưa quá hạn và không có người dùng khác yêu cầu. Việc gia hạn có thể thực hiện trực tuyến hoặc bằng máy self-check tại tầng một hoặc tầng hai. [Nguồn 1] Thông tin đối chiếu trực tiếp từ chính sách: VinUniversity ID đồng thời là thẻ thư viện để mượn tài liệu hoặc thiết bị. Sinh viên đại học được mượn tối đa 3 tài liệu, mỗi tài liệu trong 2 tuần. Sách được gia hạn một lần thêm 1 tuần nếu chưa quá hạn và không có người dùng khác yêu cầu. Có thể gia hạn trực tuyến hoặc bằng máy self-check tại tầng một hoặc tầng hai. [Nguồn 1]
- **Câu 2:** Một nhóm được đặt phòng học thư viện tối đa 2 giờ trong một phiên, 2 phiên trong một ngày và 4 phiên trong một tuần, tính chung cho tất cả phòng. Nếu người dùng đến muộn quá 10 phút, đặt chỗ sẽ bị hủy. [Nguồn 1] Thông tin đối chiếu trực tiếp từ chính sách: Mỗi nhóm được đặt tối đa 2 giờ trong một phiên, 2 phiên trong một ngày và 4 phiên trong một tuần, tính chung cho tất cả phòng. Có thể đặt trong ngày sử dụng hoặc sớm nhất 1 tuần trước đó. [Nguồn 1]
- **Câu 3:** Thiết bị thư viện được mượn trong 1 ngày làm việc. Người mượn phải trả thiết bị chậm nhất 15 phút trước giờ đóng cửa. Nếu thiết bị quá hạn trên 5 ngày, nó sẽ được xem là thất lạc và người mượn phải trả chi phí thay thế. [Nguồn 1] Thông tin đối chiếu trực tiếp từ chính sách: Thiết bị thư viện được mượn trong 1 ngày làm việc và phải trả trực tiếp tại quầy lưu hành tầng một, muộn nhất 15 phút trước giờ đóng cửa. Thiết bị quá hạn trên 5 ngày được xem là thất lạc và người mượn phải trả chi phí thay thế. [Nguồn 1]
- **Câu 4:** Sinh viên hiện tại có thể truy cập tài nguyên điện tử từ ngoài trường bằng cách sử dụng VinUni ID. Khi sử dụng máy tính công cộng, người dùng cần lưu ý đóng trình duyệt sau khi hoàn tất để tránh người khác sử dụng sai tài khoản hoặc tài nguyên. Tài nguyên điện tử phải được sử dụng cho mục đích cá nhân, phi thương mại và tuân thủ bản quyền cùng điều khoản cấp phép. [Nguồn 1] Thông tin đối chiếu trực tiếp từ chính sách: Sinh viên hiện tại có thể truy cập tài nguyên trực tuyến từ ngoài trường bằng VinUni ID. Khi dùng máy tính công cộng, người dùng phải đóng trình duyệt sau khi hoàn tất để tránh người khác sử dụng sai tài khoản hoặc tài nguyên. Tài nguyên điện tử phải được dùng cho mục đích cá nhân, phi thương mại và tuân thủ bản quyền cùng điều khoản cấp phép. [Nguồn 1]
- **Câu 5:** Sinh viên cần thực hiện các bước sau để kiểm tra và xử lý khi tài khoản vẫn báo quá hạn mặc dù đã trả sách: 1. **Xác nhận việc trả sách**: Đảm bảo rằng sách đã được trả qua máy self-check hoặc trạm trả sách 24/7 trước khi đưa lên giá. [Nguồn 1] 2. **Kiểm tra email xác nhận**: Xem lại email xác nhận trả sách từ thư viện để đảm bảo rằng việc trả sách đã được ghi nhận. [Nguồn 1] 3. **Liên hệ với nhân viên**: Nếu tài khoản vẫn chưa được cập nhật sau khi đã thực hiện hai bước trên, sinh viên cần liên hệ với nhân viên tại Information Desk để được hỗ trợ. [Nguồn 1] Thông tin đối chiếu trực tiếp từ chính sách: Trước tiên, người dùng cần xác nhận sách đã được trả qua máy self-check hoặc trạm trả sách 24/7 trước khi đưa lên giá. Tiếp theo, kiểm tra email xác nhận trả sách của thư viện. Nếu tài khoản vẫn chưa cập nhật, liên hệ nhân viên tại Information Desk. [Nguồn 1]
<!-- 2A202601089_DAOVANDA_BENCHMARK_RESULTS_END -->

### 5.5. Phân tích failure case và tác dụng của metadata

Câu 1 cố ý có dữ liệu gây nhiễu: tài liệu cao học nêu 5 tài liệu trong 1 tháng,
trong khi sinh viên đại học chỉ được mượn 3 tài liệu trong 2 tuần. Khi không lọc,
top-3 chứa `vinuni-graduate-faculty-borrowing` ở hạng 3, tạo nguy cơ agent trộn
hai định mức. Với `{"audience": "student"}`, top-3 chỉ còn hai chunk của tài
liệu sinh viên đại học và một chunk FAQ; tài liệu cao học bị loại hoàn toàn.

Kết quả này cho thấy metadata filter tăng precision cho câu hỏi có đối tượng cụ
thể. Tuy vậy, bộ lọc chỉ nên dùng khi query xác định rõ audience, vì áp dụng quá
rộng có thể làm mất tài liệu chính sách `audience=all` vẫn chứa thông tin hữu ích.

---

## Tự đánh giá

<!-- 2A202601089_DAOVANDA_SELF_ASSESSMENT_START -->
| Tiêu chí | Điểm tự đánh giá |
|---|---:|
| Khởi động | 5 / 5 |
| Hướng tiếp cận | 10 / 10 |
| Hoàn thiện code — 44/44 tests | 30 / 30 |
| Dự đoán độ tương tự và suy ngẫm | 5 / 5 |
| Kết quả truy xuất cá nhân | 10 / 10 |
| **Tổng phần cá nhân** | **60 / 60** |
<!-- 2A202601089_DAOVANDA_SELF_ASSESSMENT_END -->
